# requirements-lock.txt — EXACT versions from the validated `abm310` environment
# (Python 3.10.16). Use this for Docker / Hugging Face deployment so reviewers
# reproduce the SAME numbers you validated on.
#
# IMPORTANT: your model was validated on Mesa 3.0.3. The root requirements.txt
# still pins `mesa<3.0` (a stale constraint) — this lock supersedes it for
# deployment. geopandas 1.0.1 uses the `pyogrio` I/O engine (NOT fiona), and all
# geospatial wheels are manylinux binaries, so no system GDAL build is needed.
#
# Captured via `pip freeze` in abm310 on 2026-06-02.

Mesa==3.0.3
Mesa-Geo==0.9.1

numpy==2.2.6
pandas==2.2.3
scipy==1.15.3

geopandas==1.0.1
shapely==2.1.1
pyproj==3.7.1
pyogrio==0.11.0
rtree==1.4.0
geopy==2.4.1

scikit-learn==1.7.1
networkx==3.4.2

dash==3.0.4
dash-leaflet==1.0.15
plotly==6.1.2
Flask==3.0.3
Werkzeug==3.0.6

psutil==7.0.0
tqdm==4.67.1
SALib==1.5.2

# Production WSGI server for the container (not installed in the local env;
# only needed for deployment). Does not affect simulation results.
gunicorn==23.0.0
